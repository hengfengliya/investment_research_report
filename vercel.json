{
  "framework": "vite",
  "buildCommand": "cd backend && npm install && cd ../frontend && rm -rf node_modules package-lock.json && npm install && npm run build && cd .. && npm run prisma:generate",
  "outputDirectory": "frontend/dist",
  "installCommand": "echo 'Skipping root install'",
  "env": {
    "NODE_VERSION": "20"
  }
}
